services:
  app:
    build: .
    container_name: mini-supervision
    ports:
      - "3000:3000"
    volumes:
      - ./backend/data:/app/backend/data
    env_file:
      - ./backend/.env
    restart: unless-stopped